{
  "name": "con-monorepo",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "test": "node --test test/*.test.mjs && bash test/network.sh",
    "check": "npm exec --workspace @con/web -- tsc --noEmit && npm --workspace @con/web run build && npm test",
    "web": "npm --workspace @con/web run dev"

  }
}
