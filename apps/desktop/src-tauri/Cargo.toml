[package]
name = "helm-desktop"
version = "0.1.0"
edition = "2021"
description = "Control your coding agents on every machine you own"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }

[profile.release]
opt-level = "s"
lto = true
codegen-units = 1
strip = true
